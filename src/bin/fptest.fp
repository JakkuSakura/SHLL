// Bootstrap-friendly placeholder binary.
//
// The full fptest implementation currently relies on language/runtime features
// that the compiled backend does not support yet.

pub fn main() {
    println("fptest (bootstrap) - compiled backend smoke test");
}
