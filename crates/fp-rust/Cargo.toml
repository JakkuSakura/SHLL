[package]
name = "fp-rust"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
fp-core = { path = "../fp-core" }
fp-lang = { path = "../fp-lang" }
toml = "0.8"
bincode = "1.3"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
