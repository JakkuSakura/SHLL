#[unimplemented]
pub struct SocketAddr {
    host: str,
    port: i64,
}

impl SocketAddr {
    pub fn new(host: &str, port: i64) -> SocketAddr {
        loop {}
    }

    pub fn parse(addr: &str) -> SocketAddr {
        loop {}
    }

    pub fn to_string(&self) -> str {
        loop {}
    }
}
