[package]
name = "fp-native"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
fp-core = { path = "../fp-core" }
anyhow = "1.0"
thiserror = "1.0"
tracing = "0.1"
libc = "0.2"

[dev-dependencies]
tempfile = "3.8"
