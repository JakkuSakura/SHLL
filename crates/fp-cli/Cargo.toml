[package]
name = "fp-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "fp"
path = "src/bin/fp.rs"

[dependencies]
# Core FerroPhase crates
fp-core = { path = "../fp-core" }
fp-jit = { path = "../fp-jit" }
fp-backend = { path = "../fp-backend" }
fp-compiler = { path = "../fp-compiler" }
fp-typing = { path = "../fp-typing" }
fp-llvm = { path = "../fp-llvm", optional = true }
fp-lang = { path = "../fp-lang" }
fp-rust = { path = "../fp-rust" }
fp-bytecode = { path = "../fp-bytecode" }
fp-stackcode = { path = "../fp-stackcode" }
fp-interpret = { path = "../fp-interpret" }
fp-native = { path = "../fp-native" }
fp-cranelift = { path = "../fp-cranelift", optional = true }

# CLI and argument parsing
clap = { version = "4.4", features = ["derive", "color"] }
clap_complete = "4.4"

# File I/O and path handling
walkdir = "2.4"
globset = "0.4"

# Logging and diagnostics
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }
miette = { version = "7.6.0", features = ["fancy"] }
termwiz = "0.23.3"

# Serialization for configuration
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.9.8"

# Terminal output and colors
console = "0.16.1"
indicatif = "0.18.0"

# Async runtime for potential future features
tokio = { version = "1.0", features = ["rt", "rt-multi-thread", "fs", "process", "time", "macros"] }
futures-executor = "0.3"

# Error handling
eyre = "0.6"
thiserror = "2.0.17"

# Additional utilities
dirs = "6.0.0"
pathdiff = "0.2"
tempfile = "3.8"
quote = "1.0"
libc = "0.2"
object = { version = "0.37.3", features = ["write"] }
semver = "1"

# Performance and profiling

[dev-dependencies]
tempfile = "3.8"
assert_cmd = "2.0"
predicates = "3.0"

[features]
default = [
    "llvm",
    "cranelift",
]

llvm = ["dep:fp-llvm"]
cranelift = ["dep:fp-cranelift"]
