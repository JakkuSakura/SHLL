ast::File {
  items:
    declare fn printf(fmt: string, *args: any) -> () : fn(string, any) -> () : fn(string, any) -> ()
    pub fn main() -> () : fn() -> () : fn() -> ()
      block (13 stmts) : ()
        pub const BUFFER_SIZE: i64 : i64
          value 4096 : i64
        pub const MAX_CONNECTIONS: i64 : i64
          value 150 : i64
        pub const FACTORIAL_5: i64 : i64
          value 120 : i64
        pub const IS_LARGE: bool : bool
          value true : bool
        expr_stmt ;
          invoke : ()
            target: printf
            args:
              value "Buffer: %lldKB, factorial(5)=%lld, large=%d
" : &string
              binop / : i64
                lhs:
                  locator BUFFER_SIZE : i64
                rhs:
                  value 1024 : i64
              locator FACTORIAL_5 : i64
              locator IS_LARGE : bool
        pub struct Config : Config{buffer_size: Expr(i64), max_connections: Expr(i64)} {
          buffer_size: Expr(i64)
          max_connections: Expr(i64)
        }
        pub const DEFAULT_CONFIG: Config{buffer_size: Expr(i64), max_connections: Expr(i64)} : Config{buffer_size: Expr(i64), max_connections: Expr(i64)}
          value Config { 2 fields } : Config{buffer_size: Expr(i64), max_connections: Expr(i64)}
        expr_stmt ;
          invoke : ()
            target: printf
            args:
              value "Config: %lldKB buffer, %lld connections
" : &string
              binop / : i64
                lhs:
                  select .buffer_size [field] : i64
                    object:
                      locator DEFAULT_CONFIG : Config{buffer_size: Expr(i64), max_connections: Expr(i64)}
                rhs:
                  value 1024 : i64
              select .max_connections [field] : i64
                object:
                  locator DEFAULT_CONFIG : Config{buffer_size: Expr(i64), max_connections: Expr(i64)}
        let runtime_multiplier
          init:
            value 3 : i64
        let optimized_size
          init:
            value 8192 : i64
        let cache_strategy
          init:
            value "large" : &string
        let total_memory
          init:
            binop * : i64
              lhs:
                locator runtime_multiplier : i64
              rhs:
                intrinsic const_block : i64
                  args:
                    block (1 stmt) : i64
                      expr_stmt (value)
                        binop * : i64
                          lhs:
                            locator BUFFER_SIZE : i64
                          rhs:
                            locator MAX_CONNECTIONS : i64
        expr_stmt ;
          invoke : ()
            target: printf
            args:
              value "Const blocks: size=%lld, strategy=%s, memory=%lld
" : &string
              locator optimized_size : i64
              locator cache_strategy : &string
              locator total_memory : i64
}
