hir::Program {
    pub fn main() -> () {
        {
            pub struct Data {
                pub a: i64,
                pub b: i64,
                pub c: [u8; 16],
            }
            pub const SIZE: usize =
                24;
            pub const FIELDS: usize =
                3;
            pub const HAS_A: bool =
                true;
            pub const HAS_X: bool =
                false;
            printf("sizeof=%llu, fields=%llu
", SIZE, FIELDS);
            printf("has_a=%d, has_x=%d
", HAS_A, HAS_X);
            pub const MAX_SIZE: usize =
                64;
            pub const SIZE_OK: bool =
                true;
            pub const IS_ALIGNED: bool =
                true;
            printf("size_ok=%d, aligned=%d
", SIZE_OK, IS_ALIGNED);
            pub const MODE: &str =
                "optimized";
            printf("mode: %s
", MODE);
        }
    }
}
