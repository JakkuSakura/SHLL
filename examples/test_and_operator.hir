hir::Program {
    pub fn main() -> () {
        {
            const A: bool =
                true;
            const B: bool =
                false;
            const C: bool =
                (A && B);
            std::Println(3 args);
        }
    }
}
