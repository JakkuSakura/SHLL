hir::Program {
    pub fn main() -> _ {
        {
            let BUFFER_SIZE: u64 = 4096;
            let MAX_CONNECTIONS: i32 = 150;
            let TIMEOUT_MS: u64 = 30000;
            let FACTORIAL_5: i64 = 120;
            let IS_POWER_OF_TWO: bool = ((BUFFER_SIZE & (BUFFER_SIZE - 1)) == 0);
            let DEBUG_LEVEL: i32 = <control-flow>;
            {
            }
            let DEFAULT_CONFIG: Config = 0;
            std::io::println("Config: buffer=%luKB, connections=%d, timeout=%llums, debug=%d", (BUFFER_SIZE / 1024), MAX_CONNECTIONS, TIMEOUT_MS, DEBUG_LEVEL)
            std::io::println("Computed: factorial=%lld, is_pow2=%d", FACTORIAL_5, IS_POWER_OF_TWO)
        }
    }
}
