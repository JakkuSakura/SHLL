hir::Program {
    pub fn print(i: _) -> () {
        // params: [i: _]
        {
            printf("%s
", i)
            printf("%s
", i)
        }
    }

    pub struct BarNamedStruct {
        pub c: i64,
        pub d: i64,
    }

    pub fn main() -> () {
        {
            FooPlusBar { a: 1, b: 2, c: 3, d: 4 };
        }
    }
}
