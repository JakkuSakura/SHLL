hir::Program {
    pub const PRIME_COUNT: i64 =
        6;

    pub const ZERO_BUFFER_CAPACITY: i64 =
        16;

    pub const HTTP_STATUS_COUNT: i64 =
        4;

    pub fn main() -> () {
        {
            std::Println(0 args);
            std::Println(0 args);
            std::Println(1 args);
            std::Println(1 args);
            std::Println(0 args);
            std::Println(1 args);
        }
    }
}
