fn add(a: i64, b: i64) -> i64 {
    a + b
}
fn double(x: i64) -> i64 {
    x * 2
}
fn compose(x: i64) -> i64 {
    double(add(x, 1))
}
fn main() -> () {
    println!("{}", add(2, 3));
    println!("{}", double(5));
    println!("{}", compose(10));
    const RESULT: i64 = 30;
    println!("const: {}", RESULT);
}

// Type: ()
